.menu-page { display: flex; height: 100vh; padding-bottom: 112rpx; box-sizing: border-box; flex-direction: column; overflow: hidden; background: var(--surface); }
.menu-overview { flex: none; padding: 18rpx 26rpx 12rpx; border-bottom: 1rpx solid var(--line); background: var(--surface); }
.menu-store-row { display: flex; align-items: center; justify-content: space-between; gap: 22rpx; }
.menu-store-identity { display: flex; min-width: 0; flex: 1; align-items: center; gap: 14rpx; }
.menu-store-identity > image { width: 38rpx; height: 38rpx; flex: none; }
.menu-store-identity > view { display: flex; min-width: 0; flex-direction: column; gap: 4rpx; }
.menu-store-identity > view text:first-child { overflow: hidden; color: var(--ink); font-size: 32rpx; font-weight: var(--font-weight-semibold); text-overflow: ellipsis; white-space: nowrap; }
.menu-store-identity > view text:last-child { overflow: hidden; color: var(--muted); font-size: 22rpx; text-overflow: ellipsis; white-space: nowrap; }
.order-mode-bar { display: flex; flex: none; align-items: center; padding: 4rpx; border: 1rpx solid var(--line); border-radius: 999rpx; background: var(--paper); }
.order-mode-bar view { display: flex; align-items: center; justify-content: center; min-width: 76rpx; height: 54rpx; padding: 0 10rpx; border-radius: 999rpx; color: var(--muted); box-sizing: border-box; font-size: 23rpx; text-align: center; }
.order-mode-bar .active { color: var(--on-primary); background: var(--green); font-weight: var(--font-weight-semibold); }
.order-mode-bar .disabled { color: var(--disabled-text); }
.menu-notice-row { display: flex; align-items: center; justify-content: space-between; gap: 18rpx; margin-top: 16rpx; }
.menu-notice-row > view { display: flex; min-width: 0; flex: 1; align-items: center; gap: 12rpx; color: var(--muted); }
.menu-notice-row > view image { width: 32rpx; height: 32rpx; flex: none; opacity: .72; }
.menu-notice-row > view text { overflow: hidden; font-size: 24rpx; text-overflow: ellipsis; white-space: nowrap; }
.menu-notice-row button { display: flex; flex: none; align-items: center; height: 48rpx; margin: 0; padding: 0 4rpx 0 18rpx; background: transparent; color: var(--muted); font-size: 24rpx; line-height: 48rpx; }
.menu-closed-banner { display: flex; align-items: center; justify-content: space-between; gap: 18rpx; margin-top: 12rpx; padding: 15rpx 0 0; color: var(--ink); border-top: 1rpx solid var(--line); box-sizing: border-box; }
.closed-summary { display: flex; align-items: center; min-width: 0; }
.closed-summary > text:last-child { padding-left: 18rpx; color: var(--ink); font-size: 25rpx; }
.closed-label { padding-left: 18rpx; padding-right: 18rpx; border-left: 6rpx solid var(--green); border-right: 1rpx solid var(--line); color: var(--green); font-size: 26rpx; font-weight: var(--font-weight-semibold); }
.closed-next { flex: none; color: var(--green); font-size: 24rpx; font-weight: var(--font-weight-semibold); }
.menu-catalog { display: grid; min-height: 0; flex: 1; grid-template-columns: 170rpx minmax(0, 1fr); }
.categories { min-height: 0; height: 100%; background: var(--paper); border-right: 1rpx solid var(--line); box-sizing: border-box; }
.category { min-height: 88rpx; padding: 28rpx 18rpx; border-left: 6rpx solid transparent; box-sizing: border-box; color: var(--muted); font-size: var(--font-body); text-align: left; }
.category.active { border-left-color: var(--green); background: var(--surface, #fffefa); color: var(--green); font-weight: var(--font-weight-semibold); }
.products { min-width: 0; min-height: 0; height: 100%; background: var(--surface, #fffefa); }
.menu-title { display: flex; justify-content: space-between; padding: 32rpx 28rpx 12rpx; font-size: var(--font-subtitle); font-weight: var(--font-weight-semibold); }
.menu-title > view { display: flex; flex-direction: column; gap: 8rpx; }
.table-inline { align-self: flex-start; padding: 5rpx 12rpx; border-radius: 999rpx; background: var(--soft-primary); color: var(--green); font-size: 21rpx; font-weight: var(--font-weight-semibold); }
.fast-food-inline { align-self: flex-start; padding: 5rpx 12rpx; border-radius: 999rpx; color: #a35f27; background: #fff1dd; font-size: 21rpx; font-weight: var(--font-weight-semibold); }
.menu-title .muted { font-size: 24rpx; font-weight: 400; }
.recommended-section { flex: none; padding: 18rpx 0 14rpx; border-bottom: 1rpx solid var(--line); background: var(--surface); }
.recommended-scroll { width: 100%; white-space: nowrap; }
.recommended-track { display: inline-flex; gap: 14rpx; padding: 0 26rpx 8rpx; }
.recommended-card { display: inline-flex; width: 218rpx; flex-direction: column; gap: 10rpx; padding-bottom: 14rpx; overflow: hidden; border: 1rpx solid var(--card-border); border-radius: calc(var(--radius) * .72); background: var(--surface); box-shadow: var(--card-shadow); }
.recommended-image-wrap { position: relative; width: 218rpx; height: 164rpx; }
.recommended-image-wrap image { width: 100%; height: 100%; }
.recommended-image-wrap > text { position: absolute; top: 10rpx; left: 10rpx; padding: 5rpx 12rpx; border-radius: 8rpx; background: var(--green); color: var(--on-primary); font-size: 21rpx; font-weight: var(--font-weight-semibold); }
.recommended-name { min-height: 64rpx; padding: 0 15rpx; overflow: hidden; color: var(--ink); font-size: 27rpx; font-weight: var(--font-weight-semibold); line-height: 1.22; white-space: normal; }
.recommended-card-footer { display: flex; min-width: 0; align-items: center; justify-content: space-between; gap: 16rpx; padding: 0 14rpx; }
.recommended-card .price { color: var(--ink); font-size: 28rpx; font-weight: var(--font-weight-strong); }
.recommended-card button { display: flex; flex: none; align-items: center; justify-content: center; height: 48rpx; min-height: 48rpx; margin: 0; padding: 0; border: 1rpx solid var(--green); background: var(--green); color: var(--on-primary); box-sizing: border-box; font-size: 21rpx; font-weight: var(--font-weight-semibold); line-height: 1; }
.recommended-card button.icon-only { width: 48rpx; min-width: 48rpx; max-width: 48rpx; border-radius: 50%; font-size: 25rpx; }
.recommended-card button.with-text { width: 82rpx; min-width: 82rpx; max-width: 82rpx; border-radius: var(--button-radius); }
.recommended-card button[disabled] { background: var(--disabled-bg); color: var(--disabled-text); }
.product { position: relative; display: grid; grid-template-columns: 150rpx minmax(0, 1fr); gap: 20rpx; align-items: center; margin: 0 26rpx; padding: 26rpx 0; border-bottom: 1rpx solid var(--line); }
.density-compact .product { grid-template-columns: 118rpx minmax(0, 1fr); gap: 14rpx; padding: 17rpx 0; }
.product-image { width: 150rpx; height: 150rpx; border-radius: var(--radius, 22rpx); }
.density-compact .product-image { width: 118rpx; height: 118rpx; }
.fallback-image { object-fit: contain; background: #f3efe7; opacity: .74; }
.product-main { display: flex; min-width: 0; flex-direction: column; gap: 10rpx; }
.product-name { display: block; overflow: hidden; font-size: var(--font-subtitle); font-weight: var(--font-weight-semibold); line-height: 1.35; text-overflow: ellipsis; white-space: nowrap; }
.product-desc { overflow: hidden; color: var(--muted); font-size: var(--font-caption); text-overflow: ellipsis; white-space: nowrap; }
.product-meta { display: flex; flex-wrap: wrap; align-items: center; gap: 12rpx; color: var(--muted); font-size: var(--font-caption); }
.product-purchase { display: flex; align-items: center; justify-content: space-between; min-height: 54rpx; }
.product-purchase > text { color: var(--muted); font-size: 22rpx; }
.product-action { display: flex; flex: none; align-items: center; margin-left: auto; }
.product-action button { display: flex; flex: none; align-items: center; justify-content: center; gap: 8rpx; height: 56rpx; min-height: 56rpx; margin: 0; padding: 0; border: 1rpx solid var(--green); background: var(--green); color: var(--on-primary); box-sizing: border-box; line-height: 1; }
.product-action button.icon-only { width: 56rpx; min-width: 56rpx; max-width: 56rpx; border-radius: 50%; font-size: 28rpx; }
.product-action button.with-text { width: auto; min-width: 126rpx; max-width: none; padding: 0 18rpx; border-radius: var(--button-radius); font-size: 23rpx; }
.product-action button[disabled], .cart-bar button[disabled] { color: var(--disabled-text); background: var(--surface); border-color: var(--disabled-border); }
.selected-skus { display: flex; flex-direction: column; gap: 10rpx; margin-top: 8rpx; }
.selected-sku { display: flex; align-items: center; justify-content: space-between; gap: 12rpx; padding: 9rpx 12rpx; border-radius: 12rpx; background: var(--paper); color: var(--muted); font-size: 23rpx; }
.line-stepper { display: flex; align-items: center; gap: 10rpx; }
.line-stepper button { width: 40rpx; height: 40rpx; margin: 0; padding: 0; border: 1rpx solid rgba(120,120,120,.3); border-radius: 50%; background: var(--surface, white); font-size: 24rpx; line-height: 38rpx; }
.sold-out { color: #aaa; font-size: 22rpx; }
.category-top .menu-catalog { grid-template-columns: 1fr; grid-template-rows: 92rpx minmax(0, 1fr); }
.category-top .categories { display: block; width: 100%; height: 92rpx; white-space: nowrap; background: var(--surface, #fffefa); border-bottom: 1rpx solid rgba(120,120,120,.14); }
.category-top .category { display: inline-block; padding: 27rpx 30rpx 23rpx; }
.category-top .category.active { border-left: 0; border-bottom: 6rpx solid var(--green); }
.product-grid-view .product-list { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 18rpx; padding: 18rpx 24rpx 150rpx; }
.product-grid-view .product { display: flex; flex-direction: column; align-items: stretch; gap: 12rpx; margin: 0; padding: 0 0 18rpx; overflow: hidden; border: 1rpx solid var(--card-border); border-radius: var(--radius, 22rpx); background: var(--surface, #fffefa); box-shadow: var(--card-shadow); }
.product-grid-view .product-image { width: 100%; height: 230rpx; border-radius: 0; }
.product-grid-view .product-main { padding: 0 18rpx; }
.product-grid-view .product-purchase { margin-top: auto; }
.product-grid-view .selected-skus { display: none; }
.cart-bar { position: fixed; right: 0; bottom: 0; left: 0; z-index: 10; display: flex; align-items: center; justify-content: space-between; min-height: 104rpx; padding: 12rpx 22rpx 12rpx 28rpx; border-top: 1rpx solid var(--line); border-radius: 0; background: var(--surface); color: var(--ink); box-shadow: 0 -6rpx 20rpx rgba(23,32,27,.08); }
.cart-bar .cart-summary { display: flex; flex: 1; min-width: 0; min-height: 70rpx; align-items: center; gap: 14rpx; font-size: 21rpx; color: var(--muted); }
.cart-bar .cart-summary.can-open { cursor: pointer; }
.cart-count { display: flex; align-items: center; justify-content: center; width: 42rpx; height: 42rpx; border: 1rpx solid var(--green); border-radius: 50%; background: var(--surface); color: var(--green); font-weight: var(--font-weight-semibold); }
.cart-price { color: var(--ink); font-size: 36rpx; font-weight: var(--font-weight-strong); }
.cart-expand { margin-left: 2rpx; color: var(--green); font-size: 24rpx; font-weight: var(--font-weight-semibold); }
.cart-action { display: flex; flex: 0 0 238rpx; flex-direction: column; align-items: stretch; gap: 5rpx; margin-left: 18rpx; text-align: center; }
.cart-action > text { color: var(--muted); font-size: 20rpx; }
.cart-bar button { display: flex; align-items: center; justify-content: center; width: 100%; height: 62rpx; min-height: 62rpx; margin: 0; padding: 0 22rpx; border: 1rpx solid var(--green); border-radius: var(--button-radius); background: var(--green); color: var(--on-primary); box-sizing: border-box; font-size: var(--font-body); font-weight: var(--font-weight-semibold); line-height: 1; }
.cart-bar button[disabled] { background: var(--disabled-bg); }
.cart-sheet-mask { position: fixed; top: 0; right: 0; bottom: 104rpx; left: 0; z-index: 20; display: flex; align-items: flex-end; background: rgba(18,20,18,.46); }
.cart-sheet { width: 100%; max-height: 58vh; padding: 16rpx 28rpx 24rpx; overflow: hidden; border-radius: 30rpx 30rpx 0 0; box-sizing: border-box; background: var(--surface); box-shadow: 0 -18rpx 60rpx rgba(23,32,27,.16); }
.cart-sheet-handle { width: 66rpx; height: 7rpx; margin: 0 auto 20rpx; border-radius: 99rpx; background: var(--line); }
.cart-sheet-header { display: flex; align-items: center; justify-content: space-between; gap: 20rpx; padding-bottom: 18rpx; border-bottom: 1rpx solid var(--line); }
.cart-sheet-header > view { display: flex; align-items: baseline; gap: 12rpx; }
.cart-sheet-header > view text:first-child { color: var(--ink); font-size: 34rpx; font-weight: var(--font-weight-semibold); }
.cart-sheet-header > view text:last-child { color: var(--muted); font-size: 22rpx; }
.cart-sheet-header > button { display: flex; align-items: center; justify-content: center; width: 48rpx; min-width: 48rpx; height: 48rpx; min-height: 48rpx; margin: 0; padding: 0; border-radius: 50%; background: var(--paper); color: var(--muted); box-sizing: border-box; font-size: 30rpx; line-height: 1; }
.cart-sheet-list { max-height: calc(58vh - 112rpx); }
.cart-sheet-item { display: flex; align-items: center; justify-content: space-between; gap: 24rpx; padding: 22rpx 0; border-bottom: 1rpx solid var(--line); }
.cart-sheet-item:last-child { border-bottom: 0; }
.cart-item-copy { display: flex; min-width: 0; flex: 1; flex-direction: column; gap: 7rpx; }
.cart-item-copy text:first-child { overflow: hidden; color: var(--ink); font-size: 29rpx; font-weight: var(--font-weight-semibold); text-overflow: ellipsis; white-space: nowrap; }
.cart-item-copy text:last-child { overflow: hidden; color: var(--muted); font-size: 22rpx; text-overflow: ellipsis; white-space: nowrap; }
.cart-item-control { display: flex; flex: none; align-items: center; gap: 18rpx; }
.cart-item-control > text { min-width: 84rpx; color: var(--coffee); font-size: 28rpx; font-weight: var(--font-weight-strong); text-align: right; }
.cart-sheet-stepper { display: flex; align-items: center; gap: 12rpx; }
.cart-sheet-stepper > text { min-width: 28rpx; color: var(--ink); font-size: 23rpx; text-align: center; }
.cart-sheet-stepper button { display: flex; align-items: center; justify-content: center; width: 46rpx; min-width: 46rpx; height: 46rpx; min-height: 46rpx; margin: 0; padding: 0; border: 1rpx solid var(--line); border-radius: 50%; background: var(--surface); color: var(--ink); box-sizing: border-box; font-size: 26rpx; font-weight: var(--font-weight-semibold); line-height: 1; }
.cart-sheet-stepper button:last-child { border-color: var(--green); background: var(--green); color: var(--on-primary); }
.store-info-mask { position: fixed; inset: 0; z-index: 130; display: flex; align-items: flex-end; background: rgba(18,20,18,.48); }
.store-info-sheet { width: 100%; max-height: 78vh; padding: 18rpx 30rpx calc(32rpx + env(safe-area-inset-bottom)); overflow-y: auto; border-radius: 34rpx 34rpx 0 0; box-sizing: border-box; background: var(--surface); box-shadow: 0 -24rpx 70rpx rgba(0,0,0,.18); }
.store-info-handle { width: 70rpx; height: 7rpx; margin: 0 auto 24rpx; border-radius: 99rpx; background: var(--line); }
.store-info-title { display: flex; align-items: center; justify-content: space-between; margin-bottom: 28rpx; }
.store-info-title text { color: var(--ink); font-size: 40rpx; font-weight: var(--font-weight-semibold); }
.store-info-title button { width: 52rpx; height: 52rpx; margin: 0; padding: 0; border-radius: 50%; background: var(--paper); color: var(--muted); line-height: 52rpx; }
.store-info-section { display: flex; flex-direction: column; gap: 10rpx; padding: 22rpx 0; border-bottom: 1rpx solid var(--line); }
.store-info-section text:first-child { color: var(--ink); font-size: 30rpx; font-weight: var(--font-weight-semibold); }
.store-info-section text:not(:first-child) { color: var(--muted); font-size: 25rpx; line-height: 1.65; }
.store-info-actions { display: flex; flex-wrap: wrap; gap: 18rpx; margin-top: 28rpx; }
.store-info-actions button { flex: 1 1 180rpx; min-height: 70rpx; margin: 0; border: 1rpx solid var(--green); border-radius: var(--button-radius); background: var(--surface); color: var(--green); font-size: 24rpx; line-height: 70rpx; }
.store-info-actions button.primary { background: var(--green); color: var(--on-primary); }
.dine-in-scan-mask { position: fixed; top: 0; right: 0; bottom: 0; left: 0; z-index: 40; display: flex; align-items: flex-end; background: rgba(18, 20, 18, .42); }
.dine-in-scan-sheet { position: relative; width: 100%; padding: 20rpx 32rpx calc(30rpx + env(safe-area-inset-bottom)); border-top: 1rpx solid var(--line); border-radius: 28rpx 28rpx 0 0; background: var(--surface); box-shadow: 0 -18rpx 58rpx rgba(23,32,27,.14); box-sizing: border-box; }
.sheet-handle { width: 68rpx; height: 7rpx; margin: 0 auto 20rpx; border-radius: 99rpx; background: var(--disabled-border); }
.scan-sheet-heading { display: flex; align-items: center; justify-content: center; margin-bottom: 24rpx; }
.scan-sheet-heading > text { color: var(--ink); font-size: 36rpx; font-weight: var(--font-weight-semibold); }
.scan-sheet-heading > view { position: absolute; top: 42rpx; right: 30rpx; display: flex; align-items: center; justify-content: center; width: 52rpx; height: 52rpx; }
.scan-sheet-main { display: grid; grid-template-columns: 190rpx minmax(0, 1fr); gap: 28rpx; align-items: center; }
.scan-visual { display: flex; align-items: center; justify-content: center; height: 190rpx; border: 1rpx solid var(--line); border-radius: calc(var(--radius, 24rpx) * .65); background: var(--paper); }
.scan-visual image { width: 116rpx; height: 116rpx; }
.scan-copy { display: flex; min-width: 0; flex-direction: column; gap: 13rpx; }
.scan-title { color: var(--ink); font-size: 30rpx; font-weight: var(--font-weight-semibold); }
.scan-description { color: var(--muted); font-size: 22rpx; line-height: 1.5; }
.scan-copy button { display: flex; align-items: center; justify-content: center; width: 100%; min-height: 64rpx; margin: 0; padding: 0 18rpx; border-radius: var(--button-radius); box-sizing: border-box; font-size: 25rpx; font-weight: var(--font-weight-semibold); line-height: 1; }
.scan-primary { border: 1rpx solid var(--green); background: var(--green); color: var(--on-primary); }
.scan-secondary { border: 1rpx solid var(--green); background: var(--surface); color: var(--green); }
.scan-steps { display: block; margin-top: 28rpx; color: var(--muted); font-size: 22rpx; text-align: center; }
.scan-back { display: block; margin: 16rpx auto 0; padding: 0 20rpx; background: transparent; color: var(--green); font-size: 24rpx; line-height: 56rpx; }
.sku-mask { position: fixed; top: 0; right: 0; bottom: 0; left: 0; z-index: 30; display: flex; align-items: flex-end; background: rgba(18, 26, 22, .48); }
.sku-picker { width: 100%; max-height: 82vh; padding: 30rpx 28rpx calc(24rpx + env(safe-area-inset-bottom)); border-radius: 30rpx 30rpx 0 0; background: var(--surface, #fffefa); box-sizing: border-box; }
.sku-picker-title { display: flex; align-items: flex-start; justify-content: space-between; margin-bottom: 24rpx; }
.sku-picker-title > view { display: flex; flex-direction: column; gap: 6rpx; }
.sku-picker-title > view text:first-child { font-size: 34rpx; font-weight: var(--font-weight-semibold); }
.sku-picker-title > view text:last-child { color: var(--muted); font-size: 24rpx; }
.sku-picker-title button { width: 52rpx; height: 52rpx; margin: 0; padding: 0; border-radius: 50%; background: var(--paper); color: var(--ink); line-height: 52rpx; }
.sku-option { display: flex; align-items: center; justify-content: space-between; width: 100%; margin: 14rpx 0 0; padding: 22rpx 24rpx; border: 2rpx solid rgba(120,120,120,.2); border-radius: 18rpx; background: var(--surface, white); font-size: 26rpx; }
.sku-option text:last-child { color: var(--green); font-weight: var(--font-weight-semibold); }
.configuration-scroll { max-height: 56vh; }
.product-gallery { width: 100%; height: 330rpx; margin-bottom: 22rpx; overflow: hidden; border-radius: 22rpx; background: var(--paper); }
.product-gallery image { width: 100%; height: 100%; display: block; }
.configuration-group { padding: 12rpx 0 22rpx; border-bottom: 1rpx solid rgba(120,120,120,.12); }
.configuration-heading { display: flex; align-items: baseline; justify-content: space-between; margin-bottom: 14rpx; }
.configuration-heading text:first-child { color: var(--ink); font-size: 30rpx; font-weight: var(--font-weight-semibold); }
.configuration-heading text:last-child { color: var(--muted); font-size: 22rpx; }
.configuration-options { display: flex; flex-wrap: wrap; justify-content: space-between; }
.configuration-option-cell { flex: 0 0 48.5%; width: 48.5%; min-width: 0; margin-bottom: 12rpx; box-sizing: border-box; }
.configuration-option { display: flex; align-items: center; justify-content: space-between; width: 100%; max-width: 100%; min-width: 0; min-height: 70rpx; margin: 0; padding: 10rpx 18rpx; overflow: hidden; border: 2rpx solid rgba(120,120,120,.18); border-radius: 16rpx; background: var(--surface, white); color: var(--ink); box-sizing: border-box; font-size: 23rpx; line-height: 1.3; }
.configuration-option text:first-child { min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.configuration-option text:last-child { flex: none; margin-left: 12rpx; color: var(--green); font-size: 22rpx; }
.configuration-option.selected { border-color: var(--green); background: var(--green); color: var(--on-primary); box-shadow: none; }
.configuration-option.selected text:last-child { color: var(--on-primary); }
.configuration-confirm { display: flex; align-items: center; justify-content: space-between; gap: 20rpx; padding-top: 22rpx; }
.configuration-confirm > view { display: flex; flex-direction: column; color: var(--muted); font-size: 22rpx; }
.configuration-price { color: var(--ink); font-size: 36rpx; font-weight: var(--font-weight-strong); }
.configuration-confirm button { display: flex; flex: 1; align-items: center; justify-content: center; max-width: 330rpx; min-height: 72rpx; margin: 0; padding: 0 24rpx; border-radius: var(--button-radius); background: var(--green); color: var(--on-primary); box-sizing: border-box; font-weight: var(--font-weight-semibold); line-height: 1.2; }
